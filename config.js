// config.js - Configuration settings for LTM Manager

module.exports = {
    // Default position for world info entries
    // 0 = before Character definition
    // 1 = after Character definition
    defaultPosition: 0,
    
    // Extension name
    extensionName: "LTMM",
    
    // Version
    version: "1.0.0"
};